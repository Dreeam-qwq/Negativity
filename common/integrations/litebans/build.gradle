sourceSets {
    main {
        java.srcDirs = ['src']
        resources.srcDirs = ['resources']
    }
}

dependencies {
    compileOnly 'com.gitlab.ruany:LiteBansAPI:0.4.1'
    api(project(':common'))
}
